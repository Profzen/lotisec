# Public API base URL used by Expo app at runtime.
# If frontend and backend are deployed in one Vercel project with routePrefix /api,
# use: https://<your-main-domain>/api
EXPO_PUBLIC_API_URL=https://your-main-domain.vercel.app/api
